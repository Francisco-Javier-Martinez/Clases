<?php
    define("host","localhost");
    define("user","root");
    define("password","");
    define("database","preguntadaw2");

    // Alias con las constantes usadas por la aplicación
    if (!defined('SERVIDOR')) define('SERVIDOR', host);
    if (!defined('USUARIO')) define('USUARIO', user);
    if (!defined('PASSWORD')) define('PASSWORD', password);
    if (!defined('BBDD')) define('BBDD', database);
?>