import { SergiMierderDirective } from './sergi-mierder.directive';

describe('SergiMierderDirective', () => {
  it('should create an instance', () => {
    const directive = new SergiMierderDirective();
    expect(directive).toBeTruthy();
  });
});
