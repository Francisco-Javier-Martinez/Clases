import { DComparsasDirective } from './d-comparsas.directive';

describe('DComparsasDirective', () => {
  it('should create an instance', () => {
    const directive = new DComparsasDirective();
    expect(directive).toBeTruthy();
  });
});
