import { PComparsasPipe } from './p-comparsas.pipe';

describe('PComparsasPipe', () => {
  it('create an instance', () => {
    const pipe = new PComparsasPipe();
    expect(pipe).toBeTruthy();
  });
});
